version: '3.8'

services:
  # frontend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.frontend
  #   container_name: frontend
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=production
  #   volumes:
  #     - ./frontend:/app
  #   networks:
  #     - app-network
  #   restart: always

  flags:
    build:
      context: ./interview-analysis-system
      dockerfile: Dockerfile
    container_name: flags
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
      - HOST=0.0.0.0
    volumes:
      - ./interview-analysis-system:/app
      - flags_node_modules:/app/node_modules
    networks:
      - app-network
    restart: always

  # suggested:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.suggested
  #   container_name: suggested
  #   ports:
  #     - "3003:3003"
  #   environment:
  #     - NODE_ENV=production
  #   volumes:
  #     - ./suggested:/app
  #   networks:
  #     - app-network
  #   restart: always

  pdf_engine:
    build:
      context: ./pdf_engine
      dockerfile: Dockerfile
    container_name: pdf_engine
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - LOG_LEVEL=INFO
    volumes:
      - ./pdf_engine:/app
    networks:
      - app-network
    restart: always

volumes:
  flags_node_modules:

networks:
  app-network:
    driver: bridge

